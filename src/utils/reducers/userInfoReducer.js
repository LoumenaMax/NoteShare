import initialState from "./initialState";
import {} from "../actions/actionTypes";

export default function credentials(state = initialState.userInfo, action) {
  switch (action.type) {
    default:
      return state;
  }
}
